[pytest]
testpaths = tests
pythonpath = .
markers =
    real_vector_store: run tests against real vector_store_service (no autouse mocks)
filterwarnings =
    ignore::DeprecationWarning
    ignore:Using `httpx` with `starlette.testclient`:Warning
