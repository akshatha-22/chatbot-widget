# Production / local dev — full stack including RAG embeddings.
# CI uses requirements-ci.txt instead (no torch / sentence-transformers download).

-r requirements-ci-core.txt

# Database drivers (optional — only needed for Postgres/MySQL)
alembic==1.12.1
psycopg2-binary==2.9.9
pymysql==1.1.0

# Vector Store — Gemini embeddings + pgvector (no local ML models)
pgvector==0.3.6

# MIME magic-byte detection
python-magic==0.4.27
